apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: app
  name: app
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    io.kompose.service: app
status:
  loadBalancer: {}
